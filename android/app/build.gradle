def ktsBuild = file('build.gradle.kts')
if (ktsBuild.exists()) {
    // Delegate to Kotlin DSL build script generated by recent Flutter versions.
    apply from: ktsBuild
} else {
    throw new GradleException("Missing build.gradle.kts; cannot build Android module.")
}
